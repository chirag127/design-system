@AGENTS.md

# Design-system Claude overlay

- Review the token contract before adding a component.
- Keep behavior and a11y reusable; keep visual identity overridable and site-specific.
- Never add a hardcoded family brand skin to an atomic package.
- Use the pinned package-manager version from `packageManager` for validation.
