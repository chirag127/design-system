---
/**
 * OzProse.astro — wraps <oz-prose> for Astro.
 */
interface Props {
	class?: string
	[key: string]: unknown
}

const { class: cls, ...rest } = Astro.props
---

<oz-prose class={cls} {...rest}>
  <slot />
</oz-prose>
